type InitArgs = record { admin : opt principal };
type Result = variant { Ok; Err : text };
service : (opt InitArgs) -> {
  delete_user : () -> (Result);
  get_controlled_canister_ids : () -> (vec principal) query;
  get_shared_vault : () -> (principal) query;
  lookup_vault : (principal) -> (opt principal) query;
  notify_canister_at_capacity : () -> ();
  register_shared_vault_user : (principal) -> (Result);
  top_up : () -> (Result);
}
