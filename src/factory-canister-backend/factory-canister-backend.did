type Result = variant { Ok; Err : text };
service : {
  get_or_create_shared_vault : () -> (principal);
  get_or_create_vault : () -> (principal);
  lookup_shared_vault : (principal) -> (opt principal) query;
  lookup_vault : (principal) -> (opt principal) query;
  top_up : () -> (Result);
}
